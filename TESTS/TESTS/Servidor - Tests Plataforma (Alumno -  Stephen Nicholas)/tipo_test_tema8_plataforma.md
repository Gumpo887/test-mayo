# Tipo test — Tema 8 (plataforma)

## Instrucciones
- Lee cada pregunta y elige **una sola opción**.
- Las **respuestas correctas** están al final del documento para autocorrección.

---

## Pregunta 1
**¿Qué método en Express redirige una solicitud a otra URL?**

A. `res.json()`  
B. `res.send()`  
C. `res.redirect()`  
D. `res.route()`  

---

## Pregunta 2
**¿Qué hace el código `app.set('port', process.env.PORT || 3000);` en Express?**

A. Cambia el motor de vistas a PUG.  
B. Crea una nueva ruta.  
C. Instala dependencias.  
D. Establece el puerto en el que escuchará el servidor.  

---

## Pregunta 3
**¿Qué es un middleware en Express?**

A. Una base de datos.  
B. Un archivo de configuración.  
C. Una herramienta para crear API REST.  
D. Una función que se ejecuta antes de que la solicitud llegue al manejador final.  

---

## Pregunta 4
**¿Qué comando ejecuta todas las dependencias de un proyecto Express?**

A. `node install`  
B. `npm start`  
C. `npm install`  
D. `npm run all`  

---

## Pregunta 5
**¿Cuál de las siguientes es una estructura de directorios común en Express?**

A. `/static, /views, /lib`  
B. `/assets, /api, /public`  
C. `/views, /routes, /public`  
D. `/views, /models, /test`  

---

## Pregunta 6
**¿Qué método de respuesta convierte un objeto JavaScript en formato JSON?**

A. `res.json()`  
B. `res.send()`  
C. `res.redirect()`  
D. `res.end()`  

---

## Pregunta 7
**¿Cuál de los siguientes archivos define las dependencias del proyecto en Express?**

A. `app.js`  
B. `index.js`  
C. `package.json`  
D. `routes.js`  

---

## Pregunta 8
**¿Qué comando se usa para generar una nueva aplicación con el motor de plantillas PUG?**

A. `express --view=pug miapp`  
B. `npm install express-pug`  
C. `express --view=hbs miapp`  
D. `node install pug-view`  

---

## Pregunta 9
**¿Cómo se define un middleware global en Express?**

A. `app.get(function(req, res, next) {...});`  
B. `app.route(function(req, res, next) {...});`  
C. `app.set(function(req, res, next) {...});`  
D. `app.use(function(req, res, next) {...});`  

---

## Pregunta 10
**¿Qué comando inicializa un nuevo proyecto con npm en Node.js?**

A. `npm init`  
B. `npm install`  
C. `npm start`  
D. `npm setup`  

---

# Respuestas correctas

1. **C**  
2. **D**  
3. **D**  
4. **C**  
5. **C**  
6. **A**  
7. **C**  
8. **A**  
9. **D**  
10. **A**
